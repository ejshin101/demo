<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <settings>
        <!-- 카멜케이스 적용 -->
        <setting name="mapUnderscoreToCamelCase" value="true"/>

        <!-- 쿼리 결과 필드가 null인 경우, 누락이 되서 나오지 않게 설정-->
        <setting name="callSettersOnNulls" value="true"/>

        <!-- 쿼리에 보내는 파라미터가 null인 경우, 오류가 발생하는 것 방지 -->
        <setting name="jdbcTypeForNull" value="NULL"/>
    </settings>

<!--    <typeAliases>
        <typeAlias type="com.example.demo.dto.UserDto" alias="user"/> &lt;!&ndash; DTO클래스 alias 등록
    </typeAliases>-->

    <!--<mappers>
        <mapper resource="mapper/usermapper.xml" /> &lt;!&ndash; 매퍼 등록 &ndash;&gt;
    </mappers>-->
</configuration>